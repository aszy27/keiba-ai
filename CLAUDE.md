# keiba プロジェクト 引き継ぎメモ

2026-09-11〜09-14 に「バックテストは回収率140%なのに実戦は73%」という問題を調査した結果と、
その過程で入れた修正・残っている課題をまとめる。システム自体の説明は `README.md` を参照。

---

## 0. 2026-09-14 追記: 払戻データの列ずれ（1〜2章の3連複回収率は無効）

- `data/return_data_progress.csv` はヘッダが `tansho,fukusho,umaren,wide,umatan,sanrenpuku,sanrentan,wakuren` なのに、
  行の約91%（21,666/23,893行。2026/5以前のほぼ全行）はスクレイパーの `RETURN_COLS` 順（wakuren が3列目）で書かれていた。
  このため `evaluate_main.py` / `evaluate_dump.py` が読んでいた「3連複払戻」は、実際には**馬単の払戻**だった。2026/6以降の行だけ正しい順。
- netkeiba の結果ページ3レースで確認済み。ヘッダ通りに読むと「3連複 > 3連単」の行が8,211行あり、補正後は0行。
- ファイルは列順をそろえて書き直した（元ファイルは `return_data_progress.csv.bak_20260914`）。
  現行の `save_buffer_to_csv` は既存ヘッダの列名に合わせて追記するので、今後のスクレイプでは再発しない想定。ずれた行を書いた経路は未特定。
- 単勝・複勝は1〜2列目で位置が変わらないため、単勝/複勝回収率は元から正しい。学習（特徴量）は払戻データを使わないのでモデルへの影響はない。

### 補正後の3連複回収率（自信度60%以上、括弧内は的中率とレース数）

| 期間 | 旧モデル（〜2024学習） | 新モデル（〜2025学習） |
|---|---|---|
| 2025年 | 73.8% (15.8%, 600R) | —（学習に使用） |
| 2026/1-5 | 80.6% (18.9%, 227R) | 79.8% (18.3%, 252R) |
| 2026/6-7 | 107.0% (17.6%, 108R) | 90.5% (15.1%, 119R) |
| 2026/8- | 62.3% (12.3%, 65R) | 49.5% (10.8%, 65R) |

- 的中時の平均払戻は430〜470円で、実戦（521円）と合う。**バックテストと実戦の差は列ずれで説明がつき、モデルに3連複での優位性はない。**
  65%・70%以上に絞っても2026/1-5は70〜90%。6月以降に100%を超える閾値もあるが、30R前後なので判断できない。
- 1章の2〜5（配当の差・6月の崩れ・テスト期間への過適合の仮説）と2章の「修正後のバックテスト」表は列ずれの産物で無効。
  6月の「崩れ」は、ファイルが正しい列順の行に切り替わった時期と一致する。
- 「2025年まで学習 / 2026年テスト」の再学習（4章の2）は実施済み。2026年の成績は旧モデルと同程度かやや悪く、改善しなかった。

### 現在のデータ分割とモデル
- `race_data_2025.csv` を `data/test/` から `data/val/` に移した（学習 2012〜2025 / テスト 2026のみ）。`models/` はこの分割で学習し直したもの。
- 元のモデル（〜2024学習）は `models.bak_20260914/`、元のOOFキャッシュは `data/oof_features_cache.pkl.bak_20260914`、
  元のバックテスト結果は `result/*.bak_20260914`。
- スクリプトは `C:\Users\aassz\anaconda3\envs\keiba-ai\python.exe` で実行する（PATH の `python` は Microsoft Store のスタブで動かない）。

---

## 1. 調査の結論

### 実戦（自信度60%以上・3連複1点100円）
2026/7/18〜9/6 で 93R / 的中13 / 回収率 72.8%。記録は `result/収支.xlsx`。

### 分かったこと

1. **予測経路のバグではない。** 同じレースをバックテスト経路で流すと 70.5% で、実戦とほぼ同じだった。
   実戦予想（`predict_past/*.txt`）とバックテストの上位3頭は当時のコードで82%一致していた。
2. **的中率はバックテストどおり。** 実戦14.0% に対しバックテスト16.1%。二項分布の範囲内。
   差が出ているのは**的中時の配当**（バックテスト平均857円 → 実戦521円）。
3. **少数の高配当依存ではない。** 的中の最高配当は2,990円で、上位10本を除いても回収率は114%残る。
4. **2026年6月を境にバックテスト自体が崩れている。**
   5月以前は連続170Rの回収率が一度も102%を下回らないのに、6月以降は70〜90%。
   ブートストラップでの確率は0.3%程度なので、単なるブレではない。
   2025年の同じ夏（6〜9月）は131%なので季節性でもない。
5. **最有力の仮説（未検証）**: テストデータ（2025〜2026/5）を見ながらV1〜V8の設計・閾値60%・3連複1点を
   決めてきたため、その期間の成績が楽観的に出ている。設計を決めた後の期間では控除率並みの水準に落ちる。
   → **2025年まで学習 / 2026年のみテスト**で再学習して切り分けるのが次の一手。
6. 補足: 単勝回収72〜80%・複勝回収82〜90%はどの閾値でも100%未満。
   モデルは実質「人気上位3頭」を当てており、市場と同じ評価をしている。
   3連複の控除率25%を超えるには、オッズを使った期待値ベースの判断が必要（学習には使わず、購入判断にだけ使えばリークしない）。

### 診断ツール
`evaluate_dump.py`（`evaluate_main.py` は無変更のまま、レース単位の結果を保存する版）を実行すると、
- `result/backtest_races.csv`: レースごとの自信度・買い目・的中・払戻
- `result/live_vs_backtest.csv`: 実戦予想とバックテストの突き合わせ
- 診断1: 高配当への依存度 / 診断2: 93Rでの回収率のばらつき / 診断3: 実戦との一致率

---

## 2. 入れた修正（2026-09-11〜14）

| ファイル | 内容 |
|---|---|
| `core/features.py` | ①血統ベクトル未登録の馬を両親のベクトル平均で補完（既存馬で 自分 vs 両親平均 のコサイン類似度 中央値0.98）②騎手ID・調教師IDを5桁ゼロ埋めに統一 |
| `predict_main.py` | ①予測対象日ごとに「その日より前」の履歴だけで特徴量を作る（複数週を一括指定すると前走が消えるバグの修正）②実行前のデータ鮮度チェック（前週結果・血統/生産者マスタ・壊れたレース・馬体重・追い切り） |
| `evaluate_dump.py` | 上記の診断スクリプト（新規） |
| `repair_race_info.py` | レース情報（芝ダ・距離・天候・馬場）が欠損したレースを netkeiba から補完。偽レースの削除も行う（新規） |
| `rescrape_races.py` | 抜けている／偽レースになっているレースを結果ページから直接取り直す（新規） |

### ID書式の修正について（重要）
`race_data_*.csv` は 2014〜2025年が `1170`、2012〜2013年と2026年が `01170` と書式が違う。
本番の出馬表は `01170` を出すため、2026年の騎手・調教師成績が2026年分だけで計算されていた
（騎乗履歴の中央値 4,873件 → 326件）。`features.py` で5桁に統一して解決。
**学習データ側の特徴量も変わるので、効果を出すには再学習が必要。**

### 修正後のバックテスト（自信度60%以上）

| 期間 | 修正前 | 修正後 |
|---|---|---|
| 2025年 | 140% (16.2%) | 136% (15.8%) |
| 2026/1-5 | 170% (16.7%) | 181% (18.9%) |
| 2026/6-7 | 82% (15.1%) | 107% (17.6%) |
| 2026/8- | 57% (10.9%) | 62% (12.3%) |

2025年がほぼ不変で2026年だけ改善しており、ID書式の問題が2026年固有だったことと整合する。
8月がまだ弱い理由は未解明（1か月65Rなのでブレの範囲内かもしれない）。

---

## 3. 残っている学習/推論のズレ（重要度順）

全経路（`train_main` / `evaluate_main` / `predict_main`）のコードとデータを突き合わせて洗い出した一覧。
**1〜3は未対応。** 4以降は影響が小さいか、運用で回避する。

1. **DAE特徴量が学習時と推論時で別物**（未対応・要再学習）
   LightGBM が学習した `dae_0〜31` は5分割それぞれで別に学習したDAEの出力（どの検証にも入らない約1/6の行は0）。
   推論では全データで学習した別のDAEを使うため、潜在空間が対応していない。寄与は約3%。
   → `FEATURE_COLS` から `dae_*` を外すか、全分割で同じDAEを使う作りに変える。
2. **監督AIのスタッキングのズレ**（未対応）
   監督AIは分割モデルのOOFスコアで学習し、推論では最終モデルのスコアを使う。
   スコア分布がずれるため「自信度60%」が実際の的中率14〜16%になる一因。
   → 推論で5分割モデルの平均スコアを使うか、自信度を後段でキャリブレーションする。
3. **`race_data_*.csv` のデータ破損**（一部対応済み・再発する）
   - keibascraper が「同じ日の別レースの内容」を返すことがある（偽レース）。
     2026年で6レース確認。`db.netkeiba` 側が同じ誤りを返すため、取り直しても直らない。
     `rescrape_races.py` が `race.netkeiba` の結果ページから取り直す実装になっているが、**未検証**。
   - 8月以降の中京・9/5〜6の全レースで芝ダ・距離・天候・馬場が欠損していた（`repair_race_info.py` で修復済み）。
   - 毎週のスクレイプ後に `repair_race_info.py --dry-run` で確認するのが安全。
4. **取消・除外馬の扱い**: 学習では出走扱い（`head_count`・`race_senkou_count`・LambdaRankのグループに含まれる）、
   本番の出馬表では除外。全レースの約10%に該当。
5. **馬体重の発表前に予想すると全頭が平均値(471kg)になる**（学習は全馬に実測値）。鮮度チェックで警告する。
6. **マスタの更新漏れ**: 血統マスタ未登録の馬は種牡馬「不明」＋血統ベクトル補完不可、生産者マスタ未登録は成績0。
   予想前に `scrape_horse_ped.py` / `scrape_other_data.py` を回す。鮮度チェックで警告する。
7. `popularity` は元データに無く全期間0（ズレはないが特徴量として無駄）。
8. 問題なしを確認した項目: 格付け(G1〜OP)・芝ダ・天候・馬場・競馬場の表記、追い切り評価の分布、
   馬IDの書式、払戻データとレースIDの対応、バックテストへの未来情報の混入。

---

## 4. 次にやること

1. **`rescrape_races.py` の検証**（残6レース: 202605010308〜12, 202610010604）
   ```
   python rescrape_races.py --ids 202605010308,202605010309,202605010310,202605010311,202605010312,202610010604 --dry-run
   ```
   `[race.netkeiba]` で正しい日付・レース名・1〜3着馬が出れば `--dry-run` を外す。
   両方のページで別レースが返る場合は netkeiba 側の問題なので、偽レースとして削除したままにする。
   なお `--auto` は1日12R前提で探すため、元々11Rしかない開催日（22日ある）の12Rも候補に挙がる。これは取得失敗で正常。
2. ~~2025年まで学習 → 2026年でテスト~~ 実施済み・改善なし（0章）。
3. **券種の選び直し。** `evaluate_tickets.py`（2026-09-14 追加）で、単勝・複勝・馬連・馬単・枠連・ワイド・3連複・3連単の
   回収率を自信度の閾値ごとに測る（出力: `result/ticket_roi.csv`）。
   「3連複1点」は列ずれで140%に見えたバックテストを根拠に選んだ券種なので、根拠が消えた以上ここからやり直す。
   控除率は単勝・複勝20%、馬連系・ワイド・枠連22.5%、3連複・3連単25%。回収率がその水準前後なら優位性なしと判断する。
   ※ ワイドの払戻3つは「馬番昇順の組み合わせ」に対応する前提で集計しているため、下限・上限も併記される。
   結論がその前提に左右されないかを確認すること。
4. **3連複1点の戦略そのものを見直す。** 補正後の回収率は控除率並みなので、DAE・スタッキングのズレを直しても100%超えは見込みにくい。
   オッズを使った期待値ベースの購入判断（1章の6）を検討する。
   **判定済み（2026-09-14）: モデルがオッズ以上の情報を持つか → 持たない（2026年分）**
   - `scrape/scrape_odds.py --years 2026` で各馬の確定単勝オッズ・人気を `data/odds_data_progress.csv` に取得（途中から再開可）。
     最初の50Rで、馬番→馬IDが race_data と100%一致、1着馬のオッズ×100が単勝払戻と100%一致を確認済み。
   - `evaluate_dump.py` が馬単位のスコアを `result/backtest_horses.csv` にも保存するようにした。
   - `evaluate_odds_edge.py`: 1〜5月で条件付きロジット（レース内ソフトマックス）を学習し、6月以降で
     「オッズのみ」と「オッズ＋モデルスコア」の勝ち馬予測の対数尤度を比較する。あわせて期待値（確率×オッズ）ベースの単勝購入を検証。
     差が0付近ならモデルは市場以上の情報を持たないと判断する。確定オッズを使うため、期待値の回収率は実戦より甘く出る点に注意。
   - 結果（学習1,457R / 検証983R、ログは `odds_edge_2026.log`）:
     - 1レースあたり対数尤度: オッズのみ -1.8837 / オッズ+final_score -1.8807（差 +0.0030、95%区間 -0.0004〜+0.0064）/
       オッズ+LGB2種+Transformer 差 +0.0013（-0.0046〜+0.0075）/ モデルのみ -2.0980。
     - オッズと同時に入れると final_score の係数は -0.12±0.06 で、モデルのスコアはオッズに含まれていない情報をほぼ持たない。
     - 期待値ベースの単勝購入は、どの閾値でも対象が7〜213点しかなく、回収率の95%区間は0〜370%と広すぎて判断材料にならない。
     - 人気別の単勝回収率はすべて約58〜87%で、市場の買われ方の偏りを突く余地も見えない。
   - 結論: 今の特徴量（前走成績・騎手/調教師成績・血統・追い切り等）は、すでにオッズに織り込まれている。
     再学習・ズレ修正・券種や閾値の調整では回収率100%超えは見込めない。2024〜2025年のオッズを足しても、
     係数が負で区間の上限も+0.006程度なので、結論が変わる可能性は低い。
     続けるなら「市場が使っていない情報」（例: パドック・当日の馬場バイアス・調教タイムの詳細など）を探す必要がある。

   **終了（2026-09-14〜15）: オッズを出発点にした上積みモデルの探索 → 最終テストで基準を満たさず**
   - 判定ルール（結果を見る前に決めたもの。変えないこと）:
     - **2026-06-01以降は最終テスト専用。** 特徴量や設定の比較・選択には使わない。`evaluate_residual_model.py --split final` は候補を決めてから1回だけ実行する。
     - 開発は `--split dev`（学習 2024-01〜2025-06 / early stopping 2025-07〜12 / 報告 2026-01〜05）。
     - 「いい結果」= 最終テストで オッズのみ との対数尤度の差の95%区間が0を含まず、かつ期待値ベースの単勝で回収率100%超。
     - 候補を試し尽くしても満たさなければ、そこで止める。
   - `build_edge_dataset.py`: 2024年以降の馬単位データを `data/edge_dataset.pkl` に作る。既存特徴量（dae_*は除外）に加え、
     当日バイアス（同じ日・場・芝ダで先に行われたレースの上位3頭の枠・1角位置の、長期平均からの偏り）と、
     条件変化（距離・芝ダ・場・斤量の前走からの変化、騎手/調教師の直近30日勝率、前走の人気−着順）を持つ。
     `prev_mkt_resid` はオッズファイルの人気を使うので、オッズ取得後に作り直す。
   - `evaluate_residual_model.py`: 係数×log(オッズ由来の勝率) を固定の出発点にし、LightGBM（レース内ソフトマックスのカスタム目的関数）で
     残差だけを学習して、オッズのみとの対数尤度差と期待値ベースの単勝回収率を出す。
   - オッズは `scrape/scrape_odds_api.py --years 2024,2025,2026` で netkeiba のオッズAPI（`/api/api_get_jra_odds.html`, type=1）から
     取得中（`data/odds_api_progress.csv`: 馬番・確定単勝オッズ・人気・複勝オッズ下限/上限）。1レース1リクエストで複勝オッズも取れるため、
     HTMLから単勝オッズだけを取る `scrape_odds.py` の 2024〜2025年分は途中（約250R）で止めた。
     `data/odds_data_progress.csv`（HTML版）は2026年分が完全なので、API版の2026年分と突き合わせる検証用に残す。
   - API版には馬IDが無いため、`evaluate_residual_model.py` は「1着馬のオッズ×100 = 単勝払戻」のレースだけを使う。
     最初の50R（610頭）で、1着馬のオッズ×100=単勝払戻 100%、馬番が race_data に存在 100%、HTML版の単勝オッズと一致 99.5% を確認。
     不一致は取消・除外馬だけで、APIは オッズ-3.0 / 人気9999 / 複勝-3.0 で返す（HTML版は空欄）。
     全件取得後（2026-09-15）: 9,243R（2024: 3,346 / 2025: 3,455 / 2026: 2,442）、欠け・重複なし。
     2026年の単勝オッズはHTML版と33,675頭で100%一致。1着馬のオッズ×100=単勝払戻は9,185頭中99.7%。不一致の28頭はすべて1着同着の14R（単勝払戻が2つ）で、
     `evaluate_residual_model.py` は1着が1頭のレースだけを使うため自動で除外される。
     取得中にPCのメモリ不足（ゲーム起動中）で2回強制終了されたが、50Rごとに保存しているため再実行で続きから取れた。
   - 候補「プール乖離」: 複勝オッズから見た3着以内の確率と単勝オッズから見た勝率のずれ（`x_place`, `win_place_gap`, `place_spread`）。
     単勝と複勝の投票プールの評価の食い違いを突けるかを見る。
   - 開発期間の結果（`--split dev`、報告 2026-01〜05 の1,457R、ログ `residual_dev.log`）。オッズのみとの対数尤度の差 [95%区間]:
     既存特徴量 +0.0042 [+0.0007, +0.0073] / 当日バイアス -0.0008 / **既存+当日バイアス +0.0049 [+0.0020, +0.0078]** /
     条件変化 -0.0016 / 全部 +0.0049 [+0.0002, +0.0098] / プール乖離 -0.0012 / 全部+プール乖離 +0.0022 [-0.0002, +0.0044]。
     期待値ベースの単勝はどのセットも5か月で15〜79点しかなく、回収率の区間は0〜1120%で判断材料にならない。
   - **最終テストに進める候補は「既存+当日バイアス」1つに決めた（2026-09-15、最終テストの結果を見る前）。**
     区間が0を含まない3セットのうち、差が最大タイで区間の下限が最も高く、「全部」より特徴量が少ないため。
     この後 `--split final --sets 既存+当日バイアス` を1回だけ実行し、結果にかかわらず候補の差し替えはしない。
   - **最終テストの結果（2026-06-01以降の911R、ログ `residual_final.log`）: 不合格。**
     オッズのみ -1.8600 / 既存+当日バイアス -1.8566、差 +0.0034 [95%区間 -0.0019, +0.0085]（0を含む）。
     期待値ベースの単勝回収率は 32〜73%（10〜34点）で、オッズのみ（70〜83%）も上回らなかった。
   - 結論: 開発期間では小さな上積み（1レースあたり+0.005程度）が見えたが、最終テストでは確認できなかった。
     仮に上積みが本物でも、この大きさでは期待値1を超える馬がほとんど出ず、控除率20%を超えられない。
   - **2026-06以降の期間はこの判定で使い切った。** 同じ期間で別の候補を試すと、テスト期間に合わせて選ぶことになるので行わない。
     新しい候補を試すなら、2026-09以降に貯まるレースを新しい最終テストにする（数か月分＝1,000R程度は必要）。

   **最終テスト2（事前登録: 2026-09-15。判定まで内容を変えないこと）**
   - 目的: 開発期間で見えた「既存+当日バイアス」の小さな上積み（+0.005程度）が、まだ使っていない期間でも再現するかを確かめる。
   - 対象: 2026-09-07以降のレース。`evaluate_residual_model.py --split final2 --sets 既存+当日バイアス` を
     **1,000R以上たまってから1回だけ**実行する（1,000R未満ならスクリプトは評価せずに終わる）。週およそ70Rなので2026年12月下旬の見込み。
   - 学習 2024-01〜2026-02 / early stopping 2026-03〜2026-09-06。特徴量・ハイパーパラメータ・期間は今のコードのまま固定。
   - 判定基準は前回と同じ: オッズのみ との対数尤度の差の95%区間が0を含まない、かつ期待値ベースの単勝で回収率100%超。
   - 判定前に何か変えたくなったら、final2 は捨てて「最終テスト3」として期間と内容を登録し直す（final2 の期間を見ながら直さない）。
   - 毎週、race_data・払戻・オッズを取得しておき、判定の直前に `build_edge_dataset.py` で作り直す（メモリを数GB使う）。
   - `data/test/race_data_2026.csv` の 2026-06-06/07 の48レースで全行が2重になっていた（689行）ため、完全一致の重複行を削除した
     （元ファイルは `race_data_2026.csv.bak_20260914_dedup`）。`backtest_horses.csv` には重複がなく、オッズ判定の結果には影響しない。
5. 戦略を見直すまでは実際に賭けない（補正後のバックテストでも回収率は約80%）。

### README について（2026-09-14 更新済み）
`README.md` の「回収率176.8%」は列ずれによる数字だったため、実測値（2026年テスト期間で3連複1点78.2%、実戦72.8%）に
書き換え、「データ品質と検証」の節を追加した。

### 毎週の運用手順
```
scrape/scrape_main_data.py      # TARGET_YEAR を確認
scrape/scrape_horse_ped.py
scrape/scrape_other_data.py
scrape/scrape_odds_api.py --years 2026   # 単勝・複勝オッズ（最終テスト2の判定に必要。途中から再開可）
repair_race_info.py --dry-run   # 欠損・偽レースの確認（あれば --dry-run を外す）
train_graph_embedding.py        # 血統ベクトルを更新する場合はモデルの再学習も必要
predict_main.py                 # 鮮度チェックの警告を必ず確認する。1週分ずつ、馬体重が出てから
```

---

## 5. 作業上の注意

- `predict_main.py` の `TARGET_IDS` に複数週を入れても壊れなくなったが、
  予測日ごとに特徴量を作り直すため日数分だけ時間がかかる。
- `data/` と `models/` は `.gitignore` 済み。`*.bak_*` のバックアップも Git には入らない。
- ソースは CRLF 改行。編集時に LF へ変換しないこと。
- `evaluate_main.py` は元のまま残してある。診断用の変更は `evaluate_dump.py` 側に入れる。
